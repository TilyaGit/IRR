@*@model IRR.Core.CategoryField

<h1>Создание типа</h1>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-route-categoryId="@ViewBag.CategoryId">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">

                <label class="control-label">Категория</label>
                <input class="form-control" value="@ViewBag.CategoryItemName" disabled />
                <input type="hidden" asp-for="TypeId" value="@ViewBag.CategoryItemId" />

                <label asp-for="Name" class="control-label">Название </label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>

            </div>

            <div class="form-group">
                <input type="submit" value="+" class="btn btn-primary">
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-categoryId="@ViewBag.CategoryId">
        Обратно к списку объектов
    </a>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}*@